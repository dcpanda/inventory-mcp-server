spring:
  application:
    name: inventory-mcp-server
  ai:
    mcp:
      server:
        name: inventory-mcp-server
        request-timeout: 30s
        sse-endpoint: /mcp

logging:
  level:
    org.springframework.ai.mcp: DEBUG
